name = "First";
last = " Last";

name.at = (that) => {
    println("This : " + this);
    println("That : " + that);
    that + this;
};

xyz = name.+(last);
println(xyz);

xyz = xyz + last;
println(xyz);

println(name at last);